{
  "name": "@ephor/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Ephor shared: EphorProvider interface, domain types, viem clients, Arc addresses, and the six demo scenarios. Frozen after Phase 0 — the design session builds on this.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./provider": "./src/provider.ts",
    "./scenarios": "./src/scenarios.ts",
    "./types": "./src/types.ts",
    "./addresses": "./src/addresses.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "dependencies": {
    "viem": "^2.21.55"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
